#include<string.h>
void main()
{
	char s[30];
	int i,j,size,c=0;
	printf("\nEnter a string: ");
	gets(s);
	size=strlen(s);
	i=0,j=size-1;
	while(i<size/2)
	{
		if(s[i]==s[j])
		c++;
		i++;
		j--;
	}
	if(c==size/2)
	printf("\npalindrome");
	else
	printf("\nnot palindrome");
}
